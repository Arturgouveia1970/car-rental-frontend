import React from 'react';
import Sidebar from '../../common/sidebar/Sidebar';

const Reserve = () => (
  <>
    <Sidebar />
    <h1>Reserve Page</h1>
    <h3>This page will be to reserve chosen car</h3>
  </>
);

export default Reserve;
